<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registros del tanque</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <header>
    <nav>
      <a href="/">🏡 Inicio</a> 
      <a href="/tanque/create">📝 Ingresar nuevo registro</a>
    </nav>
  </header>

  <h1>📊 Registros del sistema de llenado</h1>

  <% if (Array.isArray(tanque) && tanque.length > 0) { %>
    <div class="container-center">
      <section class="contenedor table-panel">
        <table>
          <thead>
            <tr>
              <th>ID</th>
              <th>Nivel (cm)</th>
              <th>Tiempo (s)</th>
              <th>Litros</th>
              <th>Fecha</th>
              <th colspan="3">Acciones</th>
            </tr>
          </thead>
          <tbody>
            <% tanque.forEach(t => { %>
              <tr>
                <td><%= t.ID_TANQUE %></td>
                <td><%= t.nivel %></td>
                <td><%= t.tiempo %></td>
                <td><%= t.litros %></td>
                <td>
                  <% 
                    const raw = t.Fecha_Datos;
                    const d = raw ? new Date(typeof raw === 'string' ? raw.replace(' ','T') : raw) : null;
                  %>
                  <%= d ? d.toISOString().replace('T',' ').slice(0,16) : '—' %>
                </td>
                <td><a href="/tanque/<%= t.ID_TANQUE %>">🔍 Ver</a></td>
                <td><a href="/tanque/<%= t.ID_TANQUE %>/edit">✏️ Editar</a></td>
                <td>
                  <form action="/tanque/<%= t.ID_TANQUE %>?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit" onclick="return confirm('¿Seguro que desea eliminar este registro?')">🗑️ Eliminar</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </section>
    </div>
  <% } else { %>
    <div class="container-center">
      <p>No hay registros aún. <a href="/tanque/create">Crea el primero</a>.</p>
    </div>
  <% } %>

</body>
</html>
