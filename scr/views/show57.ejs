<!DOCTYPE html>
<html>
<head>
  <title>Detalle del Carro</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <h1>Detalle del carro</h1>
    <% if (carro) { %>
      <p><strong>ID:</strong> <%= carro.id %></p>
      <p><strong>Marca:</strong> <%= carro.marca %></p>
    <% } else { %>
      <p>Carro no encontrado.</p>
    <% } %>
    <a href="/" class="btn">Volver al inicio</a>
  </div>
</body>
</html>
