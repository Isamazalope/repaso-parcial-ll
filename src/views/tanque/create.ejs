<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ingresar datos del tanque</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <nav>
      <a href="/">🏡 Inicio</a>
      <a href="/tanque">📈 Ver registros</a>
    </nav>
  </header>

  <% if (typeof mensaje !== 'undefined' && mensaje) { %>
    <p style="color: rgb(0, 255, 255); font-weight: bold;"><%= mensaje %></p>
  <% } %>

  <h1>💧 Ingresar datos del tanque</h1>

  <div class="container-center">
    <section class="contenedor form-panel">
      <form action="/tanque" method="POST">
        <label>Nivel alcanzado (cm)</label>
        <input type="number" name="nivel" required>

        <label>Tiempo de llenado (segundos)</label>
        <input type="text" name="tiempo" required>

        <label>Litros utilizados</label>
        <input type="number" name="litros" required>

        <label>Fecha (opcional)</label>
        <input type="datetime-local" name="fecha">

        <button type="submit">Guardar</button>
      </form>
    </section>
  </div>
</body>
</html>
